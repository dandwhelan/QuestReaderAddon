# Cached listfile (~150 MB) and derived index — rebuild with: voice_sources.py index
.cache/
wowhead/

# Python
__pycache__/
*.pyc
.venv*/

# Pipeline artifacts — every one is regenerated by the step that names it,
# and the audio directories run to gigabytes.
missing.txt
slice.txt
npcs.txt
ids.txt
passages.json
passages.csv
voicebank.json
reference-audio/
generated/
